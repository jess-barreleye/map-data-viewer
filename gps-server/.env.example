# GPS Server Environment Variables
# Your machine's IP address: 192.168.0.153

# Ship (Seapath) GPS Configuration
UDP_PORT_SHIP=12345
WS_PORT_SHIP=8081

# ROV GPS Configuration
UDP_PORT_ROV=12346
WS_PORT_ROV=8082

# Bind address (0.0.0.0 listens on all interfaces)
UDP_HOST=0.0.0.0
